const container = document.querySelector('.container');

//Hapus
function hapus(){
    
};

//ambil semua kelas
const gambar = document.querySelectorAll('.thums');
gambar.forEach(thumb => {
    if（thumb.setAttribute = ）
});
